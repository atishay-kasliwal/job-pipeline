# Core dependency — job scraping
python-jobspy>=1.1.0

# Data processing
pandas>=2.0.0

# MongoDB driver
pymongo[srv]>=4.6.0

# Loads .env file automatically in local dev
python-dotenv>=1.0.0

# Used transitively by jobspy but pinned for reproducibility
requests>=2.31.0

# Claude API — used by the ATS resume analyzer
anthropic>=0.40.0
